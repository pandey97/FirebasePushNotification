<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FirebasePushNotification.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Entry
            x:Name="InputField"
            Placeholder="Enter Technician Name here"
            HorizontalOptions="Fill" />

            <Button
                x:Name="GetFcmToken"
                Text="GetFcmToken"
                Clicked="OnGetFcmTokenClicked"
                HorizontalOptions="Fill" />
            <Button
                x:Name="DeleteFcmToken"
                Text="DeleteFcmToken"
                Clicked="OnDeleteFcmTokenClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
